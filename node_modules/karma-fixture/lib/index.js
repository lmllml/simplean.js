// Generated by CoffeeScript 1.9.2
(function() {
  var adapter_path, framework, framework_path, path, pattern;

  path = require('path');

  adapter_path = path.join(__dirname, 'adapter.js');

  framework_path = path.join(__dirname, 'fixture.js');

  pattern = function(file) {
    return {
      pattern: file,
      included: true,
      served: true,
      watched: false
    };
  };

  framework = function(files) {
    files.unshift(pattern(adapter_path));
    files.unshift(pattern(framework_path));
  };

  framework.$inject = ['config.files'];

  module.exports = {
    'framework:fixture': ['factory', framework]
  };

}).call(this);
